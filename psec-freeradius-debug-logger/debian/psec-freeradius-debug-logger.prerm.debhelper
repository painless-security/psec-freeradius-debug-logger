# Automatically added by dh_systemd_start
if [ -d /run/systemd/system ]; then
	deb-systemd-invoke stop psec-freeradius-debug-logger.service >/dev/null
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/psec-freeradius-debug-logger" ] || [ -e "/etc/init/psec-freeradius-debug-logger.conf" ]; then
	invoke-rc.d psec-freeradius-debug-logger stop || exit $?
fi
# End automatically added section
